_schema-version: "3.2"
ID: appsalesorderdisplay
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appsalesorderdisplay-destination-content
  type: com.sap.application.content
  requires:
  - name: appsalesorderdisplay-destination-service
    parameters:
      content-target: true
  - name: appsalesorderdisplay-repo-host
    parameters:
      service-key:
        name: appsalesorderdisplay-repo-host-key
  - name: appsalesorderdisplay-uaa
    parameters:
      service-key:
        name: appsalesorderdisplay-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appsalesorderdisplay_html_repo_host
          ServiceInstanceName: appsalesorderdisplay-html5-service
          ServiceKeyName: appsalesorderdisplay-repo-host-key
          sap.cloud.service: appsalesorderdisplay
        - Authentication: OAuth2UserTokenExchange
          Name: appsalesorderdisplay_uaa
          ServiceInstanceName: appsalesorderdisplay-xsuaa-service
          ServiceKeyName: appsalesorderdisplay-uaa-key
          sap.cloud.service: appsalesorderdisplay
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: appsalesorderdisplay-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appsalesorderdisplay-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appsalesorderdisplay.zip
      name: appsalesorderdisplay
      target-path: resources/
- name: appsalesorderdisplay
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appsalesorderdisplay-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appsalesorderdisplay-destination-service
    service-plan: lite
- name: appsalesorderdisplay-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appsalesorderdisplay-xsuaa-service
    service-plan: application
- name: appsalesorderdisplay-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appsalesorderdisplay-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
